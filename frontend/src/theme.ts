import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        primary: { main: '#795548' },   // potato-brown
        secondary: { main: '#FFC107' }, // buttery-gold
    },
    shape: { borderRadius: 12 },
});
